name: Run test
on:
  push:
    branches:
      - main
      - master
jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@main
      - name: Run Tests
        run: ng test

        
      - name: El test ha fallado
        if: ${{ failure() }}
        uses: Thalia98/Viktorina@main
        with:
          branch: ${{ github.event.inputs.branch }}

